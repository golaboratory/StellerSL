# StellerSL

## 1. 開発の目的

PC、タブレット、スマートフォンなど、あらゆるデバイスから簡易に入力・閲覧できるよう、**レスポンシブデザインを前提**としたTODO管理アプリを開発する。
複数のプロジェクトを管理可能で、各プロジェクトには複数のタスクが含まれる。
各タスクは、一括で登録やステータス変更が可能とする。

また、自身のタスクの一覧では、タスクの総数や、未完了のタスク数、完了のタスク数が表示される。
一日のタスクの消化数や、登録数でグラフを表示する。
モチベーション維持のため、達成率に応じてバッジの獲得や、キャラクターの成長などの要素も検討する。

個人のタスク一覧では、GTDの考え方を取り入れ、簡易にステータスの変更を行えるようにする

マルチテナント方式を採用し、アクセスされるドメイン名によってテナントを識別する。
データアーキテクチャとしては各テーブルに `tenant_id` (UUID) を持たせることで、DB内でテナント間のデータを**論理分離**する前提とする。

### 1-1. 機能一覧

- プロジェクト一覧
- プロジェクト詳細
- タスク一覧
- タスク詳細
- タスク登録
- タスク更新
- タスク削除
- タスク一括登録
- タスク一括更新
- タスク一括削除
- グラフ表示
- バッジ獲得
- キャラクター成長
- ユーザー登録
- ログイン
- ログアウト
- プロジェクトへのユーザアサイン管理
- チームでのタスク管理
- チームでのタスク一括登録
- チームでのタスク一括更新
- チームでのタスク一括削除
- チームでのグラフ表示
- チームでのバッジ獲得
- チームでのキャラクター成長
- ガントチャート表示
  - ガントチャートでのタスクの追加
  - ガントチャートでのタスクの更新
  - ガントチャートでのタスクの削除
  - ガントチャートでのスケジュールの変更
- カレンダー表示
  - カレンダーでのタスクの追加
  - カレンダーでのタスクの更新
  - カレンダーでのタスクの削除

## 2. 技術スタック

- フロントエンド
  - vue v3
  - TypeScript
  - Tailwind CSS
  - PrimeVue
  - OpenAPI Generator
  - Pinia (状態管理)
  - npm
  - pnpm
- バックエンド
  - Go
  - huma
  - sqlc
  - github.com/golaboratory/gloudia
- 認証方式
  - JWT (JSON Web Token)
- データベース
  - PostgreSQL
- インフラ
  - Docker
  - Docker Compose

## 3. 開発の進め方

本プロジェクトは、小さく素早くイテレーションを回すアジャイル開発と、LLM（AI）とのペアプログラミングを前提とします。手戻りを防ぎ、型安全性を担保するために、**バックエンド先行・スキーマ駆動（Schema-Driven）**のアプローチを採用します。

### 3-1. 開発フローの全体像

1. **フェーズ1：基盤構築・PoC（概念実証）**
   - Docker Compose を用いたローカル開発環境（DB, Backend, Frontend）の構築
   - DBスキーマの設計および `sqlc` を用いたGoコード自動生成の設定
   - `huma` を用いたAPI基盤のセットアップと OpenAPI (Swagger) 仕様書の自動出力確認
   - 画面のベースレイアウト（PrimeVue + Tailwind）の作成
2. **フェーズ2：中核機能の実装（MVP）**
   - プロジェクト管理およびタスクCRUDの実装
   - タスクの一括登録・更新インターフェースの構築
   - 個人のタスク一覧画面（GTD要素を含む簡易ステータス変更）の実装
3. **フェーズ3：付加価値・拡張機能の実装**
   - グラフ表示（1日の消化数・登録数の可視化など）
   - バッジ獲得・キャラクター成長システムのロジックおよびUI実装
   - チーム機能（アサイン管理、共有タスク管理）の統合
4. **フェーズ4：テスト・調整・デプロイ**

### 3-2. 日々の実装サイクル（スキーマ駆動開発）

フロントエンドとバックエンドの連携をスムーズにするため、以下のサイクルで機能を実装します。

1. **DB設計 (SQL)**
   - テーブルスキーマ（DDL）および必要なSQLクエリを作成。
   - `sqlc` を実行し、型安全なGoのDBアクセスコードを自動生成する。
2. **API実装 (Go + huma)**
   - Service/Handler層を実装し、リクエスト／レスポンスの構造体を定義。
   - `huma` を通じてAPIエンドポイントを実装し、OpenAPI仕様書（JSON/YAML）を自動出力させる。
3. **フロントエンド連携 (Vue 3 + TypeScript)**
   - 出力されたOpenAPI仕様書から、型付きのAPIクライアントコードを生成（`gloudia` や OpenAPI Generator などを活用）。
   - PrimeVueコンポーネントを活用してUIを実装し、APIと結合する。

### 3-3. タスク管理とブランチ戦略

- **タスク管理**: GitHub Issues などを活用し、「タスク一覧の作成」「一括更新APIの実装」など、粒度を細かく分けて管理する。
- **ブランチ戦略**: GitHub Flow をベースとする。
  - `main` ブランチは常にデプロイ可能な状態を保つ。
  - 新機能追加や修正時は `feature/XXX` や `fix/XXX` ブランチを作成し、Pull Request 経由でレビュー（AIへの相談含む）後にマージする。

### 3-4. AIエージェント（LLM）との協業ルール

- 抽象的な機能依頼ではなく、**「コンポーネント単位」「API単位」** にタスクを分割してAIに実装を依頼する。
- データベースのスキーマ設計や API の入出力インターフェースを先にAIと壁打ちして決定し、そこからロジックやUIの実装へと進める。
- 実装時にエラーが発生した場合は、エラーログと関連ファイル（例: VueコンポーネントとGoハンドラー）のパスをAIに提示し、原因特定と修正を効率化する。

## 4. コーディング規約

プロジェクト全体の可読性と保守性を高めるため、以下のコーディング規約を適用します。詳細はリンターやフォーマッターの自動整形に委ね、チーム全体（LLM含む）で実装にブレが出ないようにします。

### 4-1. 全般設定事項

- **命名規則**: クラス・関数・変数は役割が明確に伝わる英名とする。
- **コメントの記載**: 原則として「何をしているか（What）」はコードそのもので表現し、コメントには「なぜその実装にしたのか（Why）」という背景・意図を残す。
- **コミットメッセージ**: `feat:` (新機能), `fix:` (バグ修正), `refactor:` (リファクタリング), `docs:` (ドキュメント) など、Conventional Commits に準じたプレフィックスを用いる。

### 4-2. フロントエンド (Vue 3 / TypeScript)

- **コンポーネント設計**: Composition API (`<script setup>`) を一貫して使用する。
- **状態管理 (State Management)**: ログイン情報など、常にクライアント側で必要となるグローバルな状態については、**Pinia** を使用し `localStorage` を格納先として永続化する前提とする。
- **型定義の徹底**: TypeScriptの恩恵を最大限活かすため、`any` の使用は原則禁止とする。APIのモデルについては、OpenAPI（Swagger）から自動生成した型を必ず利用する。
- **スタイリング (レスポンシブ前提)**: マルチデバイス対応を前提とし、基本デザイン・レイアウトは Tailwind CSS のレスポンシブユーティリティ（`md:`, `lg:` など）を活用して構築する。独自のCSS（`<style scoped>`）は、Tailwindで表現が難しいアニメーションや複雑な疑似要素などに限定する。
- **UIコンポーネント**: PrimeVueが提供している標準コンポーネントを優先的に利用し、自前での複雑なコンポーネント開発を避ける。
- **自動整形**: Prettier および ESLint を導入し、保存時にコードフォーマットが走るようにする。

### 4-3. バックエンド (Go)

- **フォーマット**: 標準ツールである `gofmt` や `goimports` を用いて、コードスタイルを統一する。
- **アーキテクチャ**: 処理の肥大化を防ぐため「Handler (API層)」「Service (ビジネスロジック層)」「Repository (DBアクセス層 / sqlc生成コード)」の責務を分けて実装する。
- **型安全なデータベースアクセス**: 生のSQL文字列生成をコード内で行わず、SQLファイルから `sqlc` を通して生成された型安全な関数を使用する。
- **エラーハンドリング**: エラーは握りつぶさず、必要に応じて `fmt.Errorf("...: %w", err)` などを用いてエラーのコンテキストを付与して上位に返す。
- **APIドキュメントとバリデーション**: `huma` によって正確な OpenAPI 仕様書が吐き出されるよう、リクエスト/レスポンス構造体には明確なタグ（`description`, `example`, その他バリデーションルール）を必ず付与する。

## 5. テストの方針

本プロジェクトでは、開発スピードと品質のバランスを両立させるため、**「コアロジックの単体テスト」と「API通信の正常性担保」に重点を置きます**。また、テストコードの雛形作成にはAIを効果的に活用し、開発コストを最小限に抑えます。

### 5-1. バックエンド (Go)

- **単体テスト (Unit Test)**:
  - Go標準の `testing` パッケージを採用します。
  - 主な対象を Service 層（ビジネスロジック）とし、タスクの状態遷移や一括更新の複雑なロジックが正しく機能することを担保します。
  - 外部依存（DBアクセス＝ Repository層）はインターフェースを利用して適宜モック化し、独立した関数の正常系・異常系のテストを記述します。
- **API・結合テスト (Integration Test)**:
  - `huma` のテストユーティリティなどを活用し、ルーティングによってリクエストを受け取り、OpenAPI仕様書に合致したレスポンスを返すかどうかのテストを実施します。
  - データベースとの連携確認が必要なクエリについては、テスト用の独立したコンテナDBなどを利用して動作を保証します。

### 5-2. フロントエンド (Vue 3 / TypeScript)

- **単体テスト**:
  - 開発ツールチェーンに含まれることが多い `Vitest` の採用を第一候補とします。
  - ロジックを持つコンポーザブル（composable 関数）や、フォームのバリデーション、タスク統計の算出など、データの加工を担う部分のテストを優先します。
  - OpenAPIから自動生成したクライアントを用いた通信部分はモック化し、UIコンポーネントがAPI依存でテスト不可になることを防ぎます。
- **E2E / UIテスト**:
  - アジャイルにUI/UXをブラッシュアップしていく前提があるため、初期（PoC〜MVP）において過度なE2Eテストは実装せず、手動やAIによる確認を中心とします。
  - 仕様が固まりプロダクトが安定してきたフェーズで、`Playwright` や `Cypress` 等の導入を検討します。

### 5-3. AIエージェント（LLM）との協業

- **テストコードを基点とした開発**: リクエスト時に「〇〇を行う関数と、その境界値チェックを含むテストを書いて」というように、実装とテストをセットでAIに依頼します。
- **既存コードに対するテスト追加**: すでにAIと作成したシステムに対して、「このコンポーネント／サービスのモックを用いたテストコード（Go / Vitest）のボイラープレートを生成して」と指示し、テスト作成の工数を削減します。

### 5-4. CI環境での自動テスト (GitHub Actions)

- **継続的インテグレーションの導入**: GitHub Flowに基づくアジャイル開発に合わせて、GitHub Actionsを利用したCIパイプラインを構築します。
- **自動テストの実行タイミング**: `main` ブランチへのPull Request（PR）作成時、およびPush時に、フロントエンド（Vitest）とバックエンド（Go `testing`）の単体テストが自動で実行されるようにします。
- **Lintおよびフォーマットチェック**: テスト実行と合わせて、コードの静的解析（Goの `golangci-lint`、Vue/TSの `ESLint` / `Prettier`）もAction内で実行し、コード品質の均一化を担保します。これにより、レビューやマージ前に必ず品質基準を満たしていることをシステム的に保証します。

## 6. デプロイの方針

本プロジェクトでは、インフラの構築コストと運用負荷を抑え、ローカル環境との差異を無くすため、**「本番（または検証）環境でも Docker Compose を利用する」** シンプルなコンテナベースのデプロイ方針を採用します。

### 6-1. 環境の分離

- 開発環境（Local）と本番/検証環境（Production/Staging）では、読み込む環境変数（`.env`）および `docker-compose.yml`（必要に応じて環境用の別ファイル等）を分けて管理します。
- データベースのクレデンシャル情報や API Secret などの機密情報は Git リポジトリの管理から除外し、サーバー上の連携ファイル・環境変数（`.env` 等）経由でコンテナへ安全に渡します。

### 6-2. 基本的なデプロイフロー

1. **コード配置**: GitHub Actions などの CI ツール、もしくはホストサーバー上でのデプロイ用スクリプトにより、対象ブランチ（`main` 等）をチェックアウトします。
2. **イメージのビルド**: `docker compose build` により、GoのバックエンドおよびVue3のフロントエンド（静的ファイル群へのビルドプロセス含む）の Docker イメージを構築します。
3. **マイグレーション**: データベースのスキーマ変更がある場合は、コンテナの起動直前・起動時にマイグレーションツールを実行しスキーマの同期を行います。
4. **サービスの起動**: `docker compose up -d` を実行し、コンテナ群をバックグラウンドで起動（再起動）させます。

### 6-3. マルチテナント（ドメイン識別）とリバースプロキシ

- 当システムはアクセスされるドメイン名からテナントを識別する要件があるため、Docker Composeネットワークの前段へ連携用に **リバースプロキシ（Nginx や Traefik 等）** のコンテナを配置します。
- SSL/TLS（HTTPS）証明書の取得および通信の終端処理はリバースプロキシ層に任せ、内部通信は HTTP とし、必要に応じてフロントエンドやAPIコンテナへルーティングを振り分けます。

### 6-4. 運用・保守（ログとバックアップ）

- 標準的なコンテナの標準出力を利用し、`docker logs` にてトラブルシューティングを行います。長期間のログ保存が必要な場合は、適宜ロギングドライバーの設定やボリュームマウントを行います。
- PostgreSQL を含む永続化データは Docker Volume へ格納し、定期的に `pg_dump` などのスクリプトを走らせてホスト外部やクラウドストレージ等へバックアップを取る仕組みを設けます。

## 7. その他

プロジェクトを円滑に進めるための補足事項や、チーム（LLM含む）間での共通認識として以下の項目を定義します。

### 7-1. 想定されるディレクトリ構造（モノレポ構成）

開発環境の統一と管理の容易さを考慮し、フロントエンドとバックエンドを同一リポジトリで管理するモノレポ構成を推奨します。

```text
.
├── backend/            # Go (huma + sqlc) バックエンド
│   ├── cmd/            # エントリポイント (main.go)
│   ├── internal/       # ビジネスロジック・APIハンドラー群
│   └── sql/            # マイグレーションファイルや sqlc 用のSQLクエリ
├── frontend/           # Vue 3 (TypeScript) フロントエンド
│   ├── src/
│   │   ├── components/ # 共通UIコンポーネント (PrimeVueベース)
│   │   ├── views/      # 画面（ページ）コンポーネント
│   │   └── api/        # OpenAPIから自動生成されたAPIクライアント
├── docker-compose.yml  # DB, Backend, Frontend などの環境定義
└── GEMINI.md           # 本プロダクトの開発・要件定義書（AIコンテキスト共有用）
```

### 7-2. AIエージェント（LLM）への効果的なプロンプト・テンプレート

LLMとのペアプログラミングを効率化するため、以下のようなフォーマットでコンテキストと要件を明確にして依頼することを推奨します。

**【AIへの依頼テンプレート例】**

> **目的**: タスク一覧画面のAPI連携とUI実装  
> **使用技術**: Vue 3 (Composition API), PrimeVue, 自動生成APIクライアント  
> **要件**:
>
> 1. `backend/internal/handler` のタスク取得API（OpenAPI）を利用すること。
> 2. ローディング中は PrimeVue の `Skeleton` コンポーネントを表示すること。
> 3. エラー時は `Toast` でユーザーに通知すること。  
>    **指示**: この要件を満たす `TaskList.vue` のコードと、それに付随する単体テスト（Vitest）のモックを含めたベースコードを出力してください。
