# TODO.md

## Phase 1: クリティカルな不具合修正（セキュリティ・データ整合性）

### 1-1. JWT認証ミドルウェアの実装
- **現状**: JWTトークンは生成されるが、エンドポイント側でトークンの検証が行われていない。`api.go` の `getAuth` はコンテキストにユーザー情報がなければデフォルト値を返す
- **対応**: Chi ミドルウェアとしてJWT検証を実装し、認証が必要な全エンドポイントに適用する
- **対象ファイル**: `backend/cmd/main.go`, `backend/internal/api/api.go`

### 1-2. SQLインジェクション脆弱性の修正
- **現状**: `main.go` の `WithTenant` 内で `fmt.Sprintf` によりテナントIDを直接SQL文字列に埋め込んでいる
- **対応**: パラメータ化クエリまたはUUIDバリデーションを実施する
- **対象ファイル**: `backend/internal/db/custom.go`

### 1-3. 平文パスワードフォールバックの削除
- **現状**: `auth/service.go` でbcrypt検証失敗時に平文パスワード比較を行っている
- **対応**: 平文比較ロジックを完全に削除する
- **対象ファイル**: `backend/internal/api/auth/service.go`

---

## Phase 2: バッジシステムの完成

### 2-1. バッジシードデータの作成
- **現状**: `badges` テーブルにデータが一切投入されておらず、バッジシステムが機能しない
- **対応**: `_initdb/07_badges.sql` を作成し、`_memo/バッジの仕様.md` に定義された全バッジを投入する
- **対象バッジ**: first_task, three_strike, rookie_planner, daily_runner_bronze/silver/gold, comeback_hero, weekend_master, task_crusher_1〜5, early_bird, midnight_owl, quick_strike, all_nighter, organizer, multi_tasker

### 2-2. バッジエンジンの未実装ロジック追加
- **現状**: `gamification/engine.go` に first_task, three_strike, task_crusher_1, early_bird, midnight_owl のみ実装済み
- **対応**: 以下のバッジロジックを追加実装する
  - `rookie_planner` — 1日に5タスク登録
  - `daily_runner_bronze/silver/gold` — 3/7/30日連続タスク完了（ストリーク追跡が必要）
  - `comeback_hero` — 7日以上空白後の復帰
  - `weekend_master` — 土日に累計10タスク完了
  - `task_crusher_2〜5` — 累計50/100/500/1000タスク完了
  - `quick_strike` — タスク作成後1時間以内に完了
  - `all_nighter` — 1日に10タスク完了
  - `organizer` — 3プロジェクト作成
  - `multi_tasker` — 1日に3プロジェクトのタスクを完了
- **対象ファイル**: `backend/internal/api/gamification/engine.go`, `backend/sql/query.sql`

### 2-3. シークレットバッジの実装
- **対応**: 仕様確認セクション [Q1](#q1-シークレットバッジの獲得条件表示仕様) の回答に基づき実装する

### 2-4. ストリーク追跡のためのスキーマ拡張
- **現状**: 連続日数(ストリーク)を追跡する仕組みがスキーマに存在しない
- **対応**: 仕様確認セクション [Q2](#q2-ストリーク追跡の実装方式) の回答に基づき実装する

---

## Phase 3: アクティビティログの実装

### 3-1. アクティビティログ書き込みの実装
- **現状**: `activity_logs` テーブルとシードデータは存在するが、アプリケーションコードから書き込みが一切行われていない
- **対応**: タスク作成時・完了時に `activity_logs` へINSERTするロジックを追加する
- **対象ファイル**: `backend/internal/api/task/service.go`, `backend/sql/query.sql`（INSERT文追加）

### 3-2. ダッシュボードのアクティビティ表示修正
- **現状**: `GetRecentActivity` クエリは `activity_logs` を参照するが、3-1が未実装のためデータが蓄積されない
- **対応**: 3-1完了後、初期データ投入スクリプトとの整合性を確認する

---

## Phase 4: 不足しているAPIエンドポイント

### 4-1. 単一タスク取得エンドポイント (GET /tasks/{id})
- **現状**: タスクはリスト取得のみで、個別取得エンドポイントがない
- **対応**: `task/handler.go` に `GET /tasks/{id}` を追加
- **対象ファイル**: `backend/internal/api/task/handler.go`, `backend/internal/api/task/service.go`

### 4-2. チーム更新・削除エンドポイント
- **現状**: チームは作成・メンバー管理のみで、チーム名変更やチーム削除ができない
- **対応**: `PUT /teams/{id}`, `DELETE /teams/{id}` を追加
- **対象ファイル**: `backend/internal/api/team/handler.go`, `backend/sql/query.sql`

### 4-3. 単一チーム取得エンドポイント (GET /teams/{id})
- **現状**: チーム詳細画面は `listTeams()` で全件取得後にフィルタリングしており非効率
- **対応**: `GET /teams/{id}` エンドポイントを追加
- **対象ファイル**: `backend/internal/api/team/handler.go`, `backend/sql/query.sql`

### 4-4. 現在のユーザー情報取得エンドポイント (GET /auth/me)
- **現状**: ログイン後に自身のプロフィールを取得するエンドポイントがない
- **対応**: `GET /auth/me` を追加してJWTからユーザー情報を返す

---

## Phase 5: タスク機能の拡充

### 5-1. タスクのフィルタリング機能
- **現状**: タスク一覧はフロントエンドでの検索のみ（タイトル/説明の部分一致）。API側のフィルタがない
- **対応**: `GET /tasks` にクエリパラメータ (`status`, `priority`, `project_id`, `due_date_from`, `due_date_to`) を追加
- **対象ファイル**: `backend/internal/api/task/handler.go`, `backend/sql/query.sql`

### 5-2. タスク担当者の変更機能
- **現状**: `assigned_to` はタスク作成時に設定されるが、後から変更するUIやエンドポイントがない
- **対応**: `PUT /tasks/{id}` で `assigned_to` を更新可能にする（既存エンドポイントの拡張）

### 5-3. タスクのステータス遷移ルールの実装
- **現状**: ステータス変更にバリデーションがなく、どのステータスからでもどのステータスにも遷移可能
- **対応**: 仕様確認セクション [Q3](#q3-タスクのステータス遷移ルール) の回答に基づき実装する

---

## Phase 6: フロントエンド品質改善

### 6-1. フォームバリデーションの強化
- **現状**: HTML5の `required` 属性のみ。メールフォーマット検証、パスワード強度チェック、カスタムエラー表示がない
- **対応**: PrimeVueのフォームバリデーション機能を活用し、各フォームにバリデーションを追加
- **対象ファイル**: `Login.vue`, `Signup.vue`, `Tasks.vue`（タスク編集ダイアログ）, `Projects.vue`

### 6-2. APIクライアントの統一
- **現状**: 一部のView (`Gantt.vue`, `CalendarView.vue`, `Signup.vue`) は `Configuration` + `basePath` を直接指定、他のViewは `axiosInstance` を使用しており不統一
- **対応**: 全Viewで `axiosInstance` を統一使用する

### 6-3. ガントチャートの編集機能実装
- **現状**: ガントチャート上でドラッグしても `console.log` のみでAPIへの反映がない
- **対応**: 日付変更時にAPIの `PUT /tasks/{id}` を呼び出してdue_dateを更新する
- **対象ファイル**: `frontend/src/views/Gantt.vue`

### 6-4. カレンダービューからのタスク編集
- **現状**: カレンダーはタスクの閲覧のみで、日付をクリックしてもタスクの編集や作成ができない
- **対応**: 日付クリック時のタスク作成/編集ダイアログを追加
- **対象ファイル**: `frontend/src/views/CalendarView.vue`

### 6-5. 不要コードの削除
- **現状**: `frontend/src/components/HelloWorld.vue` が未使用で残存
- **対応**: 削除する

### 6-6. チームメンバー追加のUX改善
- **現状**: チームメンバー追加はUUID手入力が必要（`TeamDetail.vue`）
- **対応**: プロジェクトメンバー追加と同様のユーザー検索UIに変更する
- **対象ファイル**: `frontend/src/views/TeamDetail.vue`

---

## Phase 7: テストカバレッジの拡充

### 7-1. バックエンド: サービス層のユニットテスト追加
- **現状**: バックエンドのテストはルーティング検証（OpenAPIスペック確認）がほとんどで、実際のビジネスロジックテストがない
- **対応**: 各domain (task, project, team) のservice層にモックDBを使ったユニットテストを追加

### 7-2. バックエンド: バッジエンジンのユニットテスト
- **現状**: バッジ判定ロジックのテストが一切ない
- **対応**: 全バッジ種別に対する判定テストを追加
- **対象ファイル**: `backend/internal/api/gamification/engine_test.go`（新規作成）

### 7-3. フロントエンド: Vitest単体テストの拡充
- **現状**: auth store と router のみ（計6テスト）
- **対応**: toast store, 主要コンポーネントのレンダリングテストを追加

### 7-4. E2Eテスト: チーム機能
- **現状**: チーム関連のE2Eテストが一切ない
- **対応**: チーム作成、メンバー追加/削除、招待リンクのE2Eテストを追加

### 7-5. バックエンド: マルチテナント分離テスト
- **現状**: テナント分離がテストで検証されていない
- **対応**: 異なるテナントIDでのデータアクセスが正しく分離されることを検証するテストを追加

---

## Phase 8: インフラ・運用改善

### 8-1. CORSミドルウェアの設定
- **現状**: CORS設定が一切ない。開発環境ではViteのプロキシで回避しているが、本番環境では問題になる
- **対応**: Chi のCORSミドルウェアを追加
- **対象ファイル**: `backend/cmd/main.go`

### 8-2. 構造化ログの導入
- **現状**: `fmt.Println` / `fmt.Printf` のみ
- **対応**: `slog` (Go標準) またはサードパーティのロガーを導入し、JSON形式の構造化ログに切り替え

### 8-3. 本番環境のデプロイ構成
- `Dockerfile.prod` は存在するが、デプロイ先やCI/CDパイプラインの本番向け設定が未定義
- **対応**: 仕様確認セクション [Q4](#q4-本番環境のデプロイ方針) の回答に基づき構成する

### 8-4. リフレッシュトークンの実装
- **現状**: JWTトークンは24時間で期限切れとなり、再ログインが必要
- **対応**: 仕様確認セクション [Q5](#q5-リフレッシュトークンの実装方針) の回答に基づき実装する

---

## Phase 9: 追加機能（要検討）

### 9-1. 【仕様確認】パスワードリセット機能
- **現状**: パスワード変更・リセット機能が存在しない
- **確認事項**: メール送信基盤を導入するか、管理者によるリセットのみとするかの方針決定が必要
- **想定回答**:
  - **A. メールによるセルフリセット**: SMTP連携（SendGrid, AWS SES等）でリセットリンクを送信。ユーザー体験は良いが、メール送信基盤の構築・運用コストが発生する。本番運用を見据えるなら推奨
  - **B. 管理者による手動リセット**: 管理画面またはCLIから管理者がパスワードを再設定する。小規模チーム・社内ツール用途であれば十分。実装コストが低い
  - **C. ログイン済みユーザーのパスワード変更のみ**: リセット（忘れた場合）は対応せず、ログイン状態でのみ変更可能とする。最小実装で済むが、パスワード忘れ時にはBと併用が必要
  - **D. 現時点では対応しない**: PoCフェーズでは不要と判断し、将来の優先度を決めるのみにとどめる
- **回答**: B + C

### 9-2. 【仕様確認】チームの権限管理
- **現状**: チームメンバーにはrole (owner/admin/member) が設定されるが、権限チェックが一切行われていない
- **確認事項**: 各ロールで許可される操作範囲を定義する必要がある
- **想定回答**:
  - **A. 3階層ロールモデル（推奨）**:
    - `owner`: チーム削除、メンバーのロール変更、全操作可能。チームに1人のみ
    - `admin`: メンバー追加/削除、プロジェクト作成/削除が可能。ownerの削除やロール変更は不可
    - `member`: タスクの作成/編集/ステータス変更のみ。メンバー管理やプロジェクト削除は不可
  - **B. 2階層に簡略化**: `owner`（全権限）と `member`（タスク操作のみ）に絞る。adminロールは廃止し、スキーマの `role` カラムを `owner | member` に変更
  - **C. プロジェクト単位の権限**: チームロールに加えて、プロジェクトごとに `manager` / `contributor` / `viewer` の権限を持たせる。柔軟だが実装が複雑
  - **D. 現時点では対応しない**: ロールは表示上の区分にとどめ、権限チェックは導入しない。小規模チームでは全員が全操作可能でも問題ない場合がある
- **回答**: A

### 9-3. 【仕様確認】キャラクタータイプの拡張
- **現状**: `character_type` は "default" のみ。`user_growth` テーブルにカラムは存在する
- **確認事項**: 複数キャラクタータイプの仕様（種類、見た目、切り替え条件）を定義する必要がある
- **想定回答**:
  - **A. レベル到達で自動進化**: 一定レベルごとにキャラクターの見た目が変化する（例: Lv.1 たまご → Lv.5 ひよこ → Lv.10 にわとり → Lv.20 フェニックス）。実装がシンプルで、レベルアップのモチベーション向上に直結する
  - **B. ユーザー選択式**: 初回登録時またはプロフィール画面でキャラクタータイプを選択（例: 戦士、魔法使い、忍者）。タイプごとにアイコン・レベルアップ演出が異なる。パーソナライズ感が高いが、アセット制作コストが増える
  - **C. バッジ獲得による解放**: 特定バッジを獲得するとキャラクタータイプが解放され、選択可能になる。バッジシステムとの相乗効果があるが、2-2のバッジエンジン完成が前提
  - **D. 現時点では対応しない**: "default" のみのまま、レベル表示とEXPバーに注力する。キャラクター拡張はユーザーからのフィードバック後に再検討
- **回答**: A

### 9-4. 【仕様確認】通知機能
- **現状**: アプリ内通知はPrimeVueのToastのみで、バッジ獲得時の一時的な表示に限定される
- **確認事項**: 通知履歴の保持、未読管理、プッシュ通知の要否を検討する必要がある
- **想定回答**:
  - **A. アプリ内通知センター**: `notifications` テーブルを追加し、バッジ獲得・チーム招待・タスクアサイン等をDB保存。ヘッダーにベルアイコン＋未読バッジを表示し、通知一覧ドロワーで既読管理する。Toastは引き続きリアルタイム表示用に併用
  - **B. アプリ内 + ブラウザプッシュ通知**: Aに加え、Service Worker + Web Push APIでブラウザ通知を送信。タスク期限のリマインダーなどに有効だが、Push通知サーバー（FCM等）の構築が必要
  - **C. Toast表示の改善のみ**: DB保存や通知履歴は導入せず、Toast表示の種類を増やす（バッジ獲得、レベルアップ、期限間近のアラート等）。最小工数で体験を改善できる
  - **D. 現時点では対応しない**: Toastの現状維持。ユーザー数が増えて通知ニーズが明確になった段階で再検討する
- **回答**: B

---

## 仕様確認（未回答）

以下の項目は実装着手前に方針決定が必要です。各項目の選択肢から回答を選択してください。

### Q1. シークレットバッジの獲得条件・表示仕様
- **背景**: `_memo/バッジの仕様.md` にシークレットバッジの概念（「お掃除マスター」「大晦日フィニッシャー」等）が記載されているが、実装する具体的なバッジ一覧と表示方法が未定義
- **想定回答**:
  - **A. 仕様書の例をそのまま採用**: 以下の3種を初期実装する
    - 「お掃除マスター」— タスク名に「掃除」を含むタスクを完了
    - 「大晦日フィニッシャー」— 12月31日にタスクを完了
    - 「早起きは三文の徳」— 午前5時台にタスクを3つ完了
    - 表示: バッジ一覧では「???」と表示し、獲得後に名前・アイコンを公開する
  - **B. カテゴリ別に5〜10種を新規設計**: 日付系（祝日、誕生日等）、キーワード系（タスク名に特定ワード）、行動パターン系（深夜に連続完了等）の3カテゴリから幅広く設計する。バッジ一覧に「シークレット ×N」とヒントなしで件数のみ表示
  - **C. 最小限（2〜3種）でリリースし段階的に追加**: まずは仕様書の例から2種程度を実装し、ユーザーの反応を見て追加する。表示は「???」形式
  - **D. Phase 2では対応しない**: シークレットバッジは後続フェーズに回し、通常バッジの完成を優先する
- **回答**: D

### Q2. ストリーク追跡の実装方式
- **背景**: daily_runner (3/7/30日連続) や comeback_hero (7日以上の空白後復帰) のバッジには、ユーザーの連続活動日数を追跡する仕組みが必要。現在スキーマにこの機構がない
- **想定回答**:
  - **A. `user_growth` テーブルにカラム追加（推奨）**: `current_streak` (INT), `max_streak` (INT), `last_active_date` (DATE) の3カラムを追加。タスク完了時にアプリケーション側で更新する。クエリが単純で高速。ただし、activity_logsとの二重管理になるため整合性の考慮が必要
  - **B. `activity_logs` から都度計算**: 追加カラムなし。バッジ判定時に `activity_logs` の日付を集計してストリークを算出するSQLクエリを書く。データの一元管理で整合性が高いが、ログが増えるとクエリが重くなる可能性がある
  - **C. 専用テーブル `user_streaks` を新設**: `user_id`, `streak_type`, `current_count`, `max_count`, `last_date` を持つ専用テーブルを作成。将来的に「プロジェクト別ストリーク」等の拡張にも対応しやすい。ただし現時点ではオーバーエンジニアリングの可能性がある
- **回答**: C

### Q3. タスクのステータス遷移ルール
- **背景**: 現在のステータスは `todo` / `doing` / `done` の3値。APIでは任意のステータスから任意のステータスへ遷移可能で、バリデーションがない
- **想定回答**:
  - **A. 自由遷移を許可（現状維持）**: どのステータスからでもどのステータスにも変更可能。GTDの「やり直し」や「間違えた完了の取り消し」に柔軟に対応。バルク操作との相性も良い。ただし、done → todo の遷移でEXP/バッジの巻き戻しをどう扱うかの考慮が必要
  - **B. 前進のみ許可（todo → doing → done）**: 逆遷移を禁止する。一度 done にしたタスクは変更不可。誤操作時は新しいタスクを作成する運用。シンプルだが、ユーザーの操作ミスに厳しい
  - **C. 制限付き自由遷移（推奨）**: 基本は自由遷移だが、`done` → 他ステータスに戻す場合はEXP減算とバッジ再評価を行う。データ整合性が保たれるが、実装が最も複雑
  - **D. 前進 + 1段階戻しを許可**: `todo → doing → done` の前進に加え、`doing → todo`, `done → doing` の1段階戻しのみ許可。`done → todo` の直接遷移は禁止。バランスが良いが、バルク操作時の制約が増える
- **回答**: C

### Q4. 本番環境のデプロイ方針
- **背景**: `Dockerfile.prod` (マルチステージビルド) は存在するが、デプロイ先のインフラやCI/CDパイプラインが未構成
- **想定回答**:
  - **A. クラウドコンテナサービス (AWS ECS / Google Cloud Run / Azure Container Apps)**: Docker Composeからの移行がスムーズ。オートスケール、マネージドDB (RDS/Cloud SQL) が利用可能。運用コストは月額 $20〜100 程度から開始可能
  - **B. VPS (ConoHa, さくら, Vultr等) + Docker Compose**: 現在のdocker-compose.ymlをほぼそのまま利用可能。コストが低い（月額 $5〜20）。スケーラビリティは手動管理。Let's EncryptでSSL無料取得
  - **C. PaaS (Railway, Render, Fly.io)**: Dockerfileがあればデプロイ可能。CI/CDが組み込み済みで運用が最も楽。無料枠あり。ただしカスタマイズ性に制限がある
  - **D. 当面は開発環境のみ**: 本番デプロイは後回しにし、ローカルDocker Compose環境での開発・デモに集中する
- **回答**: B

### Q5. リフレッシュトークンの実装方針
- **背景**: 現在のJWTは24時間固定の有効期限。期限切れ時はフロントエンドが401を検知してログイン画面にリダイレクトする
- **想定回答**:
  - **A. リフレッシュトークン導入（推奨）**: アクセストークン（15分〜1時間）+ リフレッシュトークン（7日〜30日）の2トークン方式。`refresh_tokens` テーブルを新設し、リフレッシュトークンはDB管理。セキュリティが向上するが、実装コストがやや高い
  - **B. トークン有効期限の延長**: 24時間 → 7日間に延長する。実装変更はJWT生成時の `ExpiresAt` を変更するのみ。セキュリティリスクは増すが、最小工数で改善できる
  - **C. スライディングセッション**: アクセストークンの有効期限を24時間のまま、APIリクエスト時に残り時間が一定以下であれば新しいトークンをレスポンスヘッダーで返却する。フロントエンドが新トークンを受け取り自動更新。中程度の実装コスト
  - **D. 現状維持（24時間固定）**: 業務時間内の利用を想定し、毎日のログインを許容する。ゲーミフィケーションの「毎日ログイン」との親和性もある
- **回答**: D
