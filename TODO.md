# TODO.md

未実装タスク一覧。完了済みタスクは [COMPLETE.md](COMPLETE.md) を参照。

---

## Phase 7: テストカバレッジの拡充

### 7-1. バックエンド: サービス層のユニットテスト追加
- **現状**: ルーティング検証 + auth/service・gamification(バッジエンジン/ストリーク)のユニットテストのみ。task/project/team のサービス層テストがない
- **対応**: 各domain (task, project, team) のservice層にモックDBを使ったユニットテストを追加

### 7-3. フロントエンド: Vitest単体テストの拡充
- **現状**: auth store と router のみ（計6テスト）
- **対応**: toast store, notification store, 主要コンポーネントのレンダリングテストを追加

### 7-4. E2Eテスト: チーム機能
- **現状**: チーム関連のE2Eテストが一切ない
- **対応**: チーム作成、メンバー追加/削除（検索UI経由）、ロール変更、招待リンクのE2Eテストを追加

### 7-5. バックエンド: マルチテナント分離テスト
- **現状**: テナント分離がテストで検証されていない（注意: 開発用DBユーザーはスーパーユーザーのため RLS が強制されない。検証には非特権DBユーザーでの接続が必要）
- **対応**: 異なるテナントIDでのデータアクセスが正しく分離されることを検証するテストを追加

---

## Phase 8: インフラ・運用改善

### 8-2. 構造化ログの導入
- **現状**: `fmt.Println` / `fmt.Printf` のみ（cmd/main.go, api/auth に計8箇所）
- **対応**: `slog` (Go標準) またはサードパーティのロガーを導入し、JSON形式の構造化ログに切り替え

### 8-3. 本番環境のデプロイ構成
- **決定事項**: Q4=B — VPS + Docker Compose + Let's Encrypt
- **現状**: `backend/Dockerfile.prod` のみ存在
- **対応**: `docker-compose.prod.yml` を作成。Nginx リバースプロキシ + Let's Encrypt + prod Dockerfile。`.env.example` で環境変数ドキュメント

---

## 既知の技術的負債（実装中に発見）

### TD-1. ゲーミフィケーション更新のエラーが握りつぶされる
- `task/service.go` の `applyCompletion` 内の各クエリは `_ =` でエラーを無視している。1文でもDBエラーが起きるとトランザクション全体がabort状態になり、タスク更新ごとロールバックされる
- **対応**: エラーを返してハンドラでログする（8-2 と合わせて対応推奨）

### TD-2. 共有レイアウトがない
- 通知ベルは Dashboard のツールバーのみに設置。各ビューがツールバーを重複実装している
- **対応**: 共通レイアウトコンポーネントを導入し、通知ベル・ナビゲーションを全画面で共有

### TD-3. バッジ進捗の「もうすぐ」表示（バッジの仕様.md のモチベーション設計）
- 達成率80%以上のバッジをハイライトするプログレスUIは未実装。未獲得バッジ一覧表示（"???"含む）も未実装
